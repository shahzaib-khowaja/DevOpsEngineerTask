apiVersion: v1
kind: ConfigMap
metadata:
  name: staging-devops-task-frontend-config
data:
  .env: |
    PORT=5000
    REACT_APP_LANDING_URL=http://example.com
    REACT_APP_WEB_URL=http://app.example.com
    REACT_APP_API_URL=http://api.example.com
