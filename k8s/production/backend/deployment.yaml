apiVersion: apps/v1
kind: Deployment
metadata:
  name: production-devops-task-backend
  namespace: default
spec:
  selector:
    matchLabels:
      app: production-devops-task-backend
  replicas: 1
  template:
    metadata:
      labels:
        app: production-devops-task-backend
    spec:
      serviceAccountName: eso-sa  # ✅ Service account with IRSA
      terminationGracePeriodSeconds: 70
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: node.kubernetes.io/instance-type
                    operator: In
                    values:
                      - t3a.medium
      containers:
        - name: production-devops-task-container-backend
          image: accountid.dkr.ecr.ap-southeast-1.amazonaws.com/imagename:115d9881
          ports:
            - containerPort: 8000
          lifecycle:
            preStop:
              exec:
                command: ["/bin/sh", "-c", "sleep 40"]
          envFrom:
            - configMapRef:
                name: production-devops-task-backend-config    # ✅ From ConfigMap
            - secretRef:
                name: production-devops-task-backend-secret        # ✅ From AWS via ExternalSecret
          resources:
            requests:
              cpu: "1000m"
              memory: "1000Mi"
            limits:
              cpu: "1500m"
              memory: "1000Mi"
